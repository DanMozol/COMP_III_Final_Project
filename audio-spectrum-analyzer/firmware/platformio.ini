[platformio]
; secrets.ini holds WiFi/API credentials as build_flags — never commit it
extra_configs = secrets.ini

[env:esp32dev]
platform  = espressif32
board     = esp32dev
framework = arduino

monitor_speed = 115200

lib_deps =
    kosme/arduinoFFT @ ^2.0.2
    bblanchon/ArduinoJson @ ^7.0.0
